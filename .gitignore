# Node
node_modules/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
venv*/

# pytest / coverage
.pytest_cache/
.coverage
htmlcov/

# Home Assistant runtime data (dev container config/ dir)
config/.storage/
config/.cache/
config/home-assistant.log*
config/home-assistant_v2.db*
config/tts/
config/.HA_VERSION
config/.ha_run.lock
config/deps/
# Stock blueprints HA rewrites into any fresh config dir (ensure_config)
config/blueprints/

# Live acceptance-test failure artifacts are normally retained under /tmp.
tests/e2e/artifacts/

# Ruff
.ruff_cache/

# macOS
.DS_Store

homeassistant:
  name: MoLight E2E
  latitude: 43.6532
  longitude: -79.3832
  elevation: 76
  unit_system: metric
  time_zone: UTC

# Only what the suites use, instead of default_config, to keep each boot short:
# the REST and config-entry APIs, the frontend (onboarding, browser smoke test),
# person for onboarding's owner, zone (zone.home marks each boot) and sun.
api:
config:
frontend:
person:
zone:
sun:

logger:
  default: warning
  logs:
    custom_components.molight: debug
    custom_components.molight_testbed: debug

molight_testbed:

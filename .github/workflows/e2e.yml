name: E2E

on:
  push:
    branches: [main, develop]
  pull_request:
  schedule:
    - cron: "30 0 * * *"
  workflow_dispatch:

concurrency:
  group: e2e-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  current:
    name: Live E2E (HA current)
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - name: Run the live acceptance suite
        run: npm run test:e2e:core
        env:
          MOLIGHT_E2E_HA_IMAGE: ghcr.io/home-assistant/home-assistant:2026.8.0

  restarts:
    name: Restart scenarios (HA current)
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - name: Run the independent restart scenarios
        run: npm run test:e2e:restarts
        env:
          MOLIGHT_E2E_HA_IMAGE: ghcr.io/home-assistant/home-assistant:2026.8.0

  scenarios:
    name: Behaviour scenarios ${{ matrix.shard }} (HA current)
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        shard: [a, b, c]
    steps:
      - uses: actions/checkout@v5
      - name: Run behaviour scenario shard ${{ matrix.shard }}
        run: npm run test:e2e:scenarios-${{ matrix.shard }}
        env:
          MOLIGHT_E2E_HA_IMAGE: ghcr.io/home-assistant/home-assistant:2026.8.0

  browser:
    name: Browser smoke (HA current)
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install the pinned Playwright test package
        run: npm ci
      - name: Run the create/edit/convert browser smoke test
        run: npm run test:e2e:browser
        env:
          MOLIGHT_E2E_HA_IMAGE: ghcr.io/home-assistant/home-assistant:2026.8.0
      - name: Upload browser failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: molight-browser-e2e-failure
          path: tests/e2e/artifacts/browser
          if-no-files-found: ignore
          retention-days: 7

  compatibility:
    name: Live E2E (${{ matrix.label }}, ${{ matrix.suite }})
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    continue-on-error: ${{ matrix.experimental }}
    strategy:
      fail-fast: false
      matrix:
        suite: [core, restarts, scenarios-a, scenarios-b, scenarios-c]
        include:
          - label: HA floor
            image: ghcr.io/home-assistant/home-assistant:2026.1.0
            experimental: false
          - label: HA current
            image: ghcr.io/home-assistant/home-assistant:2026.8.0
            experimental: false
          # Floating stable is an upstream canary: report incompatibilities
          # without blocking the known-good floor/current compatibility legs.
          - label: HA stable canary
            image: ghcr.io/home-assistant/home-assistant:stable
            experimental: true
    steps:
      - uses: actions/checkout@v5
      - name: Run the ${{ matrix.suite }} suite
        run: npm run test:e2e:${{ matrix.suite }}
        env:
          MOLIGHT_E2E_HA_IMAGE: ${{ matrix.image }}

  upgrade:
    name: Previous-release upgrade E2E
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Run the previous-release upgrade suite
        run: npm run test:e2e:upgrade
        env:
          MOLIGHT_E2E_HA_IMAGE: ghcr.io/home-assistant/home-assistant:2026.8.0

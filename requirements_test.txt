# Pinned: this package pins the whole pytest stack (pytest, pytest-asyncio,
# pytest-cov, ...) and the Home Assistant version it tracks, so this one pin
# fixes them all. CI also runs the declared floor and the latest release —
# see the pytest matrix in .github/workflows/ci.yml.
pytest-homeassistant-custom-component==0.13.356
# Pinned: `select = ["ALL"]` means new ruff releases can add rules and break CI
ruff==0.15.20

{
  "name": "molight",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "hass:up": "docker compose up",
    "hass:down": "docker compose down",
    "hass:pull": "docker compose pull",
    "dev:up": "devcontainer up --workspace-folder .",
    "dev:stop": "docker stop $(docker ps -q --filter \"label=devcontainer.local_folder=$PWD\")",
    "dev:down": "docker rm -f $(docker ps -aq --filter \"label=devcontainer.local_folder=$PWD\")",
    "dev:rebuild": "devcontainer up --workspace-folder . --remove-existing-container",
    "dev:shell": "devcontainer exec --workspace-folder . bash",
    "dev:hass": "devcontainer exec --workspace-folder . bash scripts/develop",
    "test": "devcontainer exec --workspace-folder . /opt/molight-venv/bin/pytest",
    "test:e2e": "bash scripts/e2e",
    "test:e2e:upgrade": "bash scripts/e2e-upgrade",
    "lint": "devcontainer exec --workspace-folder . /opt/molight-venv/bin/ruff check custom_components tests scripts",
    "lint:fix": "devcontainer exec --workspace-folder . /opt/molight-venv/bin/ruff check --fix custom_components tests scripts",
    "format": "devcontainer exec --workspace-folder . /opt/molight-venv/bin/ruff format custom_components tests scripts",
    "format:check": "devcontainer exec --workspace-folder . /opt/molight-venv/bin/ruff format --check custom_components tests scripts",
    "release": "python3 scripts/release"
  },
  "devDependencies": {
    "@devcontainers/cli": "^0.87.0"
  }
}
